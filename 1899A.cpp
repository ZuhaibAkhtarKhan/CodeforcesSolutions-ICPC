#include <bits/sdtc++.h>
using namespace std;

int main() {
    short int t, n, turns = 0;
    cin >> t;

    while(t--) {
        cin >> n;
        bool vaTurn = true;
        while(n%3 != 0 && turns <= 10) {
            if()
        }
    }
}